# Screenshots used by the README

| File                 | What it shows                                            |
| -------------------- | -------------------------------------------------------- |
| `overlay.png`        | Caption Mode reading a video, with the text options open |
| `toolbar-button.png` | The Caption Mode button in the action row, next to Share |
| `feed-icon.png`      | A search result on hover, with the icon at the top-left  |
| `feed-hover.png`     | The icon still in place while YouTube's preview plays    |

Keep replacements at the same names and near the same widths (about 1400px,
and 557px for `feed-hover.png`). Save as optimised PNG so the repository stays
small.
